/**
 * Purpose: Chart widget placeholder for analytics panels.
 */
import React from "react";

const ChartWidget = ({ title }) => (
  <div className="card">
    <h4>{title}</h4>
    <div className="map-placeholder">Chart.js visualization</div>
  </div>
);

export default ChartWidget;
